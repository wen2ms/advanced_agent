[project]
name = "advanced-agent"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "langchain-openai>=1.6.0",
    "langgraph>=1.2.11",
]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["ALL"]
ignore = [
  "E203",
  "E266",
  "E501",
  "D",
  "T201",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
